ELEVENLABS_API_KEY=sk_f1538f4dc790e6c459e4303e8b420c5ce9983d9a3a0d215b
EW_BRAIN_KEY=7d7b965d9cfdd130a8db0150b385027ebd07344a38cb89ff
REP_MODE=raw

# BILL_ON_27B_2026_09_02 — Bill (rep want-list voice) moved off the 9B onto the
# Qwen3.8-27B-FP8 on the AMD box (.104, 2x R9700 TP=2), reached over the existing
# ew-27b-tunnel.service (.104:8001 -> C1 127.0.0.1:18001, loopback only).
# ElevenLabs TTS + Google STT unchanged. bill_watcher (the 754 SMS leg) never
# calls the LLM at all and is untouched.
# Roll back: delete the two lines below, then `systemctl restart ew-rep-voice`
# (rep_wantlist_server.py defaults fall back to the 9B at brain.experience-wholesale.net).
EW_BRAIN_URL=http://127.0.0.1:18001
EW_BRAIN_MODEL=anna
